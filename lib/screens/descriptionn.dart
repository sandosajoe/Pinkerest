import 'package:flutter/material.dart';

class descriptionn extends StatelessWidget {
  const descriptionn({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(backgroundColor:Colors.red),
    );
  }
}